from abc import ABC, abstractmethod
import math

class Shape(ABC):
    @abstractmethod
    def area(self):
        pass

class Circle(Shape):
    def __init__(self, radius):
        self.radius = radius

    def area(self):
        return self.side ** 2

shapes = [Circle(5), Square(4), Circle(10)]

for s in shapes:
    print(f"The area is: {s.area():.2f}")